var century, year, month, dayOfMonth, dayOfWeek, day;

function getUserInput (){
  century = parseInt(document.getElemetById("century").value);
  year = parseInt(document.getElemetById("year").value);
  month = parseInt(document.getElemetById("month").value);
  dayOfMonth = parseInt(document.getElemetById("day-of-month").value);
}
